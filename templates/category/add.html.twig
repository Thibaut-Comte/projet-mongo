{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form) }}
    <button type="submit">Submit</button>
    {{ form_end(form) }}

    <a href="{{ path('app_category_all') }}">Les catégories</a>
{% endblock %}