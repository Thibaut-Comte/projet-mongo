<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-5">
    <a class="navbar-brand" href="{{ path('app_product_all') }}">Cocofé</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03"
            aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_product_all') }}">{{ 'title.product'|trans }}<span
                            class="sr-only">(current)</span></a>
            </li>
            {% if app.user and is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_product_create') }}">{{ 'title.product_add'|trans }}</a>
                </li>
            {% endif %}

            {% if app.user and is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="#">{{ 'title.category'|trans }} </a>
                </li>
            {% endif %}

            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="#">{{ 'title.profile'|trans }} </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>